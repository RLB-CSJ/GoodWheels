import db from '../models/MODEL NAME'

const bikeController = {};

bikeController.getBike = (req, res, next) => {
    // Database call - probs unneeded
}

bikeController.createBike = (req, res, next) => {
    // Database call
}

export default bikeController